server:
  port: 20001
  # 把错误信息添加到页面
  error:
    include-message: always
spring:
  application:
    name: coupon-calculation-serv
logging:
  level:
    com.yejunyu.coupon: debug
# 开启所有management端点
management:
  endpoints:
    web:
      exposure:
        include: "*"
